{
  "mcpServers": {
    "code-search": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "exec \"$(git rev-parse --show-toplevel 2>/dev/null || echo .)/.mcp-server-wrapper.sh\""
      ]
    }
  }
}
